{
  "name": "@mrnkr/redux-saga-toolbox",
  "version": "1.0.14",
  "description": "Tools to make redux saga users more productive than before",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rm -rf dist && tsc",
    "do-publish": "yarn build && yarn publish --access=public",
    "test": "jest",
    "fix-deps": "yarn remove redux redux-saga && yarn add redux redux-saga -P",
    "update-promise-queue": "yarn remove @mrnkr/promise-queue && yarn add @mrnkr/promise-queue && yarn fix-deps"
  },
  "keywords": [
    "redux",
    "redux-saga",
    "saga",
    "entity",
    "helpers",
    "tools"
  ],
  "author": "Mr.NKR",
  "license": "MIT",
  "repository": "https://github.com/mrnkr/redux-saga-toolbox",
  "dependencies": {
    "@mrnkr/promise-queue": "^1.1.0",
    "lodash": "^4.17.11",
    "reselect": "^4.0.0"
  },
  "devDependencies": {
    "@types/jest": "^24.0.11",
    "@types/lodash": "^4.14.123",
    "jest": "^24.7.1",
    "redux-saga-test-plan": "v4.0.0-beta.2",
    "rxjs": "^6.5.2",
    "ts-jest": "^24.0.2",
    "ts-node": "^8.1.0",
    "tslint": "^5.16.0",
    "tslint-config-airbnb": "^5.11.1",
    "typescript": "^3.4.5"
  },
  "peerDependencies": {
    "redux": "^4.0.1",
    "redux-saga": "^1.0.2"
  },
  "jest": {
    "roots": [
      "<rootDir>/tests"
    ],
    "transform": {
      "^.+\\.tsx?$": "ts-jest"
    },
    "testRegex": "(/__tests__/.*|(\\.|/)(test|spec))\\.(ts)?$",
    "moduleFileExtensions": [
      "ts",
      "js"
    ],
    "collectCoverage": true,
    "coverageReporters": [
      "text"
    ]
  }
}
